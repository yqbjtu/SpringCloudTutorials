
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7700/eureka/

server:
  port: 6601

spring:
  application:
    name: user-service

security:
  user:
    password: none
  basic:
    enabled: false
